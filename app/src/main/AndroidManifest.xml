<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.androidvolley">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:name=".App"
        android:allowBackup="true" android:label="@string/app_name"
        android:icon="@drawable/ic_launcher" android:theme="@style/AppTheme">

        <activity android:name=".ui.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"></action>
                <category android:name="android.intent.category.LAUNCHER"></category>
            </intent-filter>
        </activity>

        <activity android:name=".ui.BaseActivity"></activity>
        <activity android:name=".ui.SimpleRequetActivity"></activity>
        <activity android:name=".ui.GsonRequestActivity"></activity>
        <activity android:name=".ui.JsonRequestActivity"></activity>
        <activity android:name=".ui.ParamsRequestActivity"></activity>
        <activity android:name=".ui.ImageLoadingActivity"></activity>
    </application>

</manifest>
